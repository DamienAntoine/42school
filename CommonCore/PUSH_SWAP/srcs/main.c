int main(int argc, char **argv)
{
    void    *astack;
    void    *bstack;

    if (argc < 2)
	    return (0);
    if (!check_input(argv))
	    exit_error(NULL, NULL);
    bstack = NULL;
    astack =
}
